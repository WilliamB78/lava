{% if app.user %}
    <ul class="nav">
        {% if is_granted('ROLE_ADMIN') -%}
            {% include 'navbar/navbarAdmin.html.twig' %}
        {% elseif is_granted('ROLE_SECRETARY') -%}
            <li class="nav-item">
                <a class="nav-link active" href="#">Secretary</a>
            </li>
        {% elseif is_granted('ROLE_USER') -%}
            {% include 'navbar/navbarUser.html.twig' %}
        {% else %}
            <li class="nav-item">
                <a class="nav-link" href="#">Link</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#">Link</a>
            </li>
            <li class="nav-item">
                <a class="nav-link disabled" href="#">Disabled</a>
            </li>
        {%- endif %}
    </ul>
{% endif %}
